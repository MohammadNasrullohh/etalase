# Frontend Stage

Role: `[Frontend Engineer]`.

- `docs/architecture/ARCHITECTURE.md` for feature boundaries.
- `docs/product/DESIGN_SYSTEM.md` for visual/component work.
- `docs/product/PRODUCT.md` for product intent.
- `theme`, `uiux-auditor`, or `image-to-code` only when triggered.
- Inspect the owning feature, adjacent shared components, and installed dependencies.
- Verify appearance and interaction visually when they change.
