# Backend Stage

Role: `[Backend Engineer]`.

- Inspect the owning feature and adjacent contracts before editing.
- Read `docs/architecture/ARCHITECTURE.md` for boundaries; read one ADR only when rationale matters.
- Load `bug-fix` for failures or regressions.
- Keep changes scoped to the owning router, service, repository, schema, or worker surface.
