# Explainer Stage

Role: `[Explainer / Tech Writer]`.

- Use Graphify first for codebase relationships; use `explain` for deep teaching mode.
- Ground answers in concrete symbols and the smallest relevant active doc.
- Use diagrams only when they materially clarify the flow.
