---
name: theme
description: Use for Lawet Hub theme presets, CSS tokens, ThemeProvider, PIN/auth styling, and theme admin UI.
---

# Theme

Read `docs/product/DESIGN_SYSTEM.md`, then use existing theme interfaces and CSS custom properties. Keep tokens centralized and avoid hardcoded visual values in components.
